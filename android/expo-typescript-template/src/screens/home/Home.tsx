import * as React from 'react';
import { Text, View } from 'react-native';

interface HomeProps { }

const Home = (props: HomeProps) => {
    return (
        <View>
            <Text>Home</Text>
        </View>
    );
};

export default Home;