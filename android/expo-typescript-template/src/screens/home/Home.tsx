import * as React from 'react';
import { Text, View, StyleSheet } from 'react-native';

interface HomeProps { }

const Home = (props: HomeProps) => {
    return (
        <View>
            <Text>Home</Text>
        </View>
    );
};

export default Home;